import { EventRegistrations } from './event-registrations';

describe('EventRegistrations', () => {
  it('should create an instance', () => {
    expect(new EventRegistrations()).toBeTruthy();
  });
});
